# 2. 알고리즘 (의사 코드) (10점)

## 1. `main` 함수의 전체 흐름
(프로젝트의 전체 작동 로직을 의사 코드로 작성하세요.)

(예시 - 관리 프로그램의 경우)
```
시작
   랜덤 시드 설정
    1~100 사이의 정답 숫자 생성
    시도 횟수를 0으로 초기화

    반복 (사용자가 정답을 맞출 때까지):
        숫자 입력 요청
        사용자 입력 받기
        시도 횟수 증가

        만약 입력 숫자가 정답보다 크면:
            "너무 큽니다." 출력
        만약 입력 숫자가 정답보다 작으면:
            "너무 작습니다." 출력
        만약 입력 숫자가 정답과 같으면:
            "정답입니다!" 출력
            반복 종료
    반복 끝

    시도 횟수 출력
종료

```

## 2. 핵심 기능 1: (예: 학생 등록 함수)
```
(핵심 기능 1의 논리적 절차를 작성하세요.)

예시:
시작
    사용자로부터 숫자 입력받기
    시도 횟수 증가

    만약 입력 숫자가 정답보다 크면:
        "너무 큽니다." 출력
    아니고 만약 입력 숫자가 정답보다 작으면:
        "너무 작습니다." 출력
    아니고 만약 입력 숫자가 정답이면:
        "정답입니다!" 출력
종료
```

## 3. 핵심 기능 2: (예: 학생 검색 함수)
```
시작
랜덤 시드 설정 (현재 시간 기반)
    1에서 100 사이의 정답 숫자 생성
    정답 변수에 저장
종료
```
